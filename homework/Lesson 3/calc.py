first_int = float(input('A = '))
second_int = float(input('B = '))

summ = first_int + second_int
diff = first_int - second_int
multi = first_int * second_int

print(float(summ), float(diff), float(multi), sep='\n')