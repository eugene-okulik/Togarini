first_int = float(input('First number = '))
second_int = float(input('Second number = '))

mean_arif = (first_int + second_int) / 2
mean_geom = (first_int * second_int) ** 0.5

print(float(mean_arif), float(mean_geom), sep='\n')
